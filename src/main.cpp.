// 1. LAS PREPARACIONES (Siempre arriba)
#include <Geode/Geode.hpp>
using namespace geode::prelude;

// 2. EL CONTENIDO (Lo que querés que haga el mod)
$on_mod(Loaded) {
    log::info("¡Mod lisi445 cargado con éxito!");
}

// 3. SI TENÉS MÁS CÓDIGO (Como modificar el menú), va después de lo anterior.
